# TradeEZ 开发规范

新会话开始前先读这份文档，可跳过大量试错。

同目录另有三份文档（**不入库**，仅存在于开发机本地）：
`TradeEZ-需求文档.md` 完整需求、`开发进度.md` 进度总表、`部署运维.md` 发版与服务器信息。

## 快速上手

```bash
cd tradeEZ
pnpm install
pnpm dev            # http://localhost:5173，自动先构建 shared
```

演示账号 `demo@tradeEZ.cn` / `abcd1234`（登录页已预填），或点微信登录（约 6 秒自动完成扫码）。

| 命令 | 说明 |
|---|---|
| `pnpm dev` | 启动前端（含 MSW 模拟数据） |
| `pnpm dev:api` | 启动后端 |
| `pnpm -r typecheck` | 全量类型检查 |
| `pnpm -r lint` | 全量 lint |
| `pnpm -r test` | 全量测试 |
| `./scripts/deploy.sh` | 发版到生产 |

**提交前必须四项全绿**：typecheck / lint / test / build。

## 仓库结构

```
tradeEZ/
├── apps/
│   ├── web/          前端（React 19 + Vite + Tailwind v4）
│   └── api/          后端（NestJS + Drizzle + Postgres）
├── packages/shared/  前后端共享的 zod schema 与领域类型
├── deploy/           Docker Compose + Caddyfile + Dockerfile
└── scripts/deploy.sh 一键发版
```

## 技术栈与选型理由

| 层 | 选型 | 为什么 |
|---|---|---|
| 前端框架 | React 19 + Vite | 登录后的重交互后台，无 SEO 需求，SSR 是负担 |
| 样式 | Tailwind v4 | 令牌集中在 `index.css` 的 `@theme inline` |
| 图表 | ECharts（已装未接） | 需求含日历热力图与 K 线，Recharts 覆盖不到；2300 点散点 SVG 会掉帧 |
| 数据 | TanStack Query | 缓存与请求状态 |
| 状态 | Zustand + persist | 筛选器、主题、语言、UI 偏好 |
| Mock | MSW | 网络层拦截，业务代码写真实 fetch |
| 后端 | NestJS + Fastify | 19 个模块的规模需要模块化与 DI |
| ORM | Drizzle | SQL 优先，写窗口函数不用绕 ORM |
| 数据库 | PostgreSQL 17 | 金额用 numeric、窗口函数、未来可加 TimescaleDB |

## 硬性约定

违反这些会引入难查的 bug，已经踩过。

### 金额一律用 numeric，禁止浮点

价格 `numeric(20,8)`、金额 `numeric(18,4)`、数量 `numeric(18,8)`。
浮点误差在数千笔累加后会让净盈亏对不上账。

postgres-js 配置为**保留 numeric 的字符串形态**，所以后端取出来的金额是 `string`，
运算需用 decimal 库或在 SQL 内完成。

### 颜色只用语义令牌，不写 slate/white

组件里写 `bg-card` / `text-fg` / `border-line` / `bg-shell-900`，
不写 `bg-white` / `text-slate-500`。明暗主题各给一组变量值，
切换主题时只换 `index.css` 里的变量，无需给每处补 `dark:` 变体。

令牌清单见 `apps/web/src/index.css` 的 `:root` 与 `.dark`。

例外：顶栏与深色导航条在两套主题下**都是深色**，其上的半透明遮罩要写死
`bg-white/12`，不能用 `bg-card/12`（深色主题下会变成深色叠深色）。

### 界面文案走 i18n 字典，不硬编码

字典在 `apps/web/src/i18n/locales.ts`，组件用 `const t = useT()` 取。
导航配置存 `labelKey` 而非 `label`，渲染时才查字典。

`TransDict` 类型从中文字典自动推导，英文缺任何键都会编译报错。

品牌字标 `tradeEZ` 是固定资产，**不进字典**，中英文下都一样。

### 前后端共用一份 zod schema

校验规则写在 `packages/shared`，前端表单与后端 DTO 都 import 它。
消除「前端改了字段忘告知后端」这类 bug。

### 业务代码走真实 fetch，mock 在网络层

不写假的 service 函数。`api/client.ts` 发 `fetch('/api/...')`，
MSW 在 Service Worker 层拦截。后端就绪后置 `VITE_USE_MOCK=false` 即可，调用点不改。

## 踩过的坑

按类型归组，避免重复踩。

### 构建与工具链

- **TS 6 弃用 `baseUrl`** —— 路径别名只保留 `paths`
- **`erasableSyntaxOnly` 禁止构造函数参数属性** —— 改显式字段声明
- **NestJS 的 DI 依赖装饰器元数据** —— `apps/api` 不能开 `erasableSyntaxOnly`（与其他包不同）
- **`moduleResolution: nodenext` 要求相对 import 带 `.js` 后缀**
- **pnpm 默认拦截 postinstall** —— msw 与 esbuild 需在 `pnpm-workspace.yaml` 的 `allowBuilds` 放行
- **vitest 会把 `dist/` 里编译后的测试副本也跑一遍** —— 需配 include/exclude，否则测试数虚高一倍

### shared 包

- **exports 必须指向编译产物而非 `.ts`** —— Vite 与 tsc 能处理 TS 源码，但 Node 运行时不能，
  API 容器一旦 import 就崩。`tsconfig` 用 `nodenext` 强制相对 import 带扩展名，让编译期就拦住
- **`pnpm dev` 前置构建 shared** —— 其 exports 指向 `dist`，新克隆直接跑会找不到模块

### MSW

- **相对路径 handler 在 Node 环境无从解析**（按 `document.baseURI` 解析）—— 测试需 jsdom 环境
- **mock 的 `delay()` 在测试中被真实等待** —— 抽 `mocks/latency.ts`，测试环境下归零（11.9s → 0.9s）
- **`VITE_USE_MOCK` 判断必须带 `import.meta.env.DEV` 前提** —— `.env.development` 只在 dev 模式加载，
  生产构建里该变量是 undefined，只判断 `!== 'false'` 会让 MSW 在线上启动并白屏
- **不要用「刷新页面 + sessionStorage 标记」等待 SW 接管** —— 标记清除时机稍有疏漏就会永久卡住，
  MSW 静默失效、所有 `/api` 返回 502。改用监听 `controllerchange` 事件

### CSS

- **`overflow-y-auto` 会同时裁剪横向** —— CSS 规定一轴非 visible 时另一轴也裁剪。
  侧栏浮窗需用 `fixed` + `createPortal` 脱离裁剪
- **`group-hover:` 只对 `.group` 的后代生效** —— 写在 `.group` 元素自身上不生效，要用 `hover:`
- **两个元素拼接的胶囊必然露缝** —— 圆角与直角之间会露出底色，改为一体渲染
- **动画关键帧被删会导致元素永久残留** —— `onAnimationEnd` 不触发。改 CSS 时注意关键帧与工具类成对存在

### 部署

- **生产镜像不应含 devDependency** —— 迁移用 drizzle-orm 自带 migrator，不用 drizzle-kit
- **Caddy 的 `handle` 是终结性路由** —— 块外的 `header` 指令进不了该链路，缓存头要写在 `handle` 内
- **Vite 产物是 `name-HASH.js`（连字符）** —— 缓存匹配按 `/assets/*` 路径而非哈希正则

## 协作流程

一个页面一轮，每轮四步：

1. **对需求** —— 补充/确认该页需求，回写进需求文档
2. **定方案** —— 说明新增哪些组件、接口、指标，指出需要决策的点
3. **开发** —— 实现页面 + mock 接口 + 必要单测
4. **收尾** —— 跑四项验证，在开发进度追加记录

需求未确认前不写页面代码。

## 当前待办

见 [开发进度.md](开发进度.md) 的「技术债」与「待确认决策」两节。
下一步是**逐页填充业务页面**，建议顺序：仪表盘 → 交易列表 → 交易详情抽屉 → 日视图。
